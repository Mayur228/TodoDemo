<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="75dp"
    android:layout_height="110dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <View
        android:id="@+id/selectedDateV"
        android:layout_width="56dp"
        android:layout_height="104dp"
        android:background="@drawable/ic_selected_date_bg"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/dayTV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Sun"
        android:layout_marginHorizontal="10dp"
        android:layout_marginVertical="25dp"
        android:textColor="@color/white"
        android:textSize="15sp"
        android:fontFamily="@font/poppins_light"
        app:layout_constraintTop_toTopOf="@id/selectedDateV"
        app:layout_constraintStart_toStartOf="@id/selectedDateV"
        app:layout_constraintEnd_toEndOf="@id/selectedDateV"
        />

    <TextView
        android:id="@+id/dateTV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="25"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:fontFamily="@font/poppins_semi_bold"
        app:layout_constraintTop_toBottomOf="@id/dayTV"
        app:layout_constraintStart_toStartOf="@id/dayTV"
        app:layout_constraintEnd_toEndOf="@id/dayTV"
        android:layout_marginTop="4dp" />

</androidx.constraintlayout.widget.ConstraintLayout>